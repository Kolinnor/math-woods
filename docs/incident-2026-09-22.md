# Error audit — 22 September 2026

Read-only production audit from midnight Europe/Paris (21 September 22:00 UTC)
through approximately 17:05 Paris. The app had been running for six days and
the public health endpoint returned `{"ok":true}`. No PostgreSQL ERROR/FATAL
entries appeared in this interval.

One browser report: #283, `/concepts/new`, at 16:59 Paris, digest 1913588724.
The corresponding server error was an alias collision with the concept Sphere.
The alias namespace validation correctly refused the collision, but threw a
generic Error that bypassed the creation form's expected validation feedback.

Fix: a typed alias conflict for canonical titles, aliases and old URLs, localized
inline feedback on creation/editing, and manual creation-form dispatch to retain
uncontrolled fields after rejection. Existing namespace locks and transactional
rollback remain in place. Unknown failures and redirects are still rethrown.

Other app logs: three malformed server-action IDs (`x`) at 12:51 Paris, with no
matching browser report; KaTeX warnings about pasted invisible Unicode characters
and array column declarations. These logs do not establish a site outage or a
rendering crash. No blanket suppression or automatic content rewrite was added.

Validation: TypeScript, core suite, and actual form components in Chromium/WebKit
with mocked server responses (FR/EN). Covers preserved title, aliases, body and
language, disabled pending controls, retry and translation-confirmation submitters.
The tests exercise all three alias collision sources and keep unexpected failures
observable. No production data or report review status was changed by this audit.
